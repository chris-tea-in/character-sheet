# Development

Install dependencies with `npm ci`. For a public clone without restricted reference data, use `PUBLIC_BUILD_MODE=1 npm run dev` or `PUBLIC_BUILD_MODE=1 npm run build`.

Run `npm run test` and `npm run typecheck:functions` before review. The standard data build requires separately authorized local data and is not available from this repository.
