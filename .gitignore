# Dependencies
node_modules/

# Build output
dist/

# Compiled data — generated by build-data.js, do not commit
public/data/
public/sql-wasm.wasm
data/

# Vite
*.local
.vite/

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude Code config / local agent tooling
.claude/

# Env
.env
.env.*
!.env.example

# Cloudflare local dev state + local-only vars (e.g. DEV_EMAIL bypass)
.wrangler/
.dev.vars

# Scraper
scraper/

# Mobile UX screenshot artifacts (scripts/mobile-shots.mjs)
mobile-shots/

# Sync-verification screenshot artifacts (scripts/verify-sync.mjs)
verify-shots/

# Stray spell→class data artifact (not part of the build pipeline)
_spells_classes.json

# misclellaneous docs that are not part of the build pipeline
docs/*

# Internal working docs — keep local, don't publish to the public GitHub repo
CLAUDE.md
bugs.md
BACKLOG.md
# AI task-observer working log (skill-improvement notes, internal by nature)
skill-observations/

# Wrangler config state (local-only, not part of the build pipeline)
wrangler.toml
